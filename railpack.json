{
  "$schema": "https://schema.railpack.com",
  "deploy": {
    "startCommand": "python -m gunicorn app:server --bind 0.0.0.0:${PORT:-8050} --workers 1 --threads 4 --timeout 120"
  },
  "packages": {
    "python": "3.12.3"
  }
}
